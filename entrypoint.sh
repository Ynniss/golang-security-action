#!/bin/sh

# if [ -z "$CODE_PATH" ] && [ -z "$CONF_PATH" ]
# then
#     gosec -conf $CONF_PATH $CODE_PATH
# elif  [ -z "$CODE_PATH" ]
# then
#     gosec $CODE_PATH
# elif  [ -z "$CONF_PATH" ]
# then
#     gosec -conf $CONF_PATH
# fi

gosec